##
##  Makefile -- Build procedure for WebKit Apache module
##  Autogenerated via ``apxs -n unknown -g''.
##

##   the used tools
APXS=/usr/sbin/apxs
#APXS=apxs
APACHECTL=apachectl

#   additional user defines, includes and libraries
#DEF=-Dmy_define=my_value
#INC=-Imy/include/dir
#LIB=-Lmy/lib/dir -lmylib

#   the default target
all: mod_webkit.so

#   compile the DSO file
mod_webkit.so: mod_webkit.c marshal.c
	$(APXS) -o mod_webkit.so -c $(DEF) $(INC) $(LIB) mod_webkit.c marshal.c

#   install the DSO file into the Apache installation
#   and activate it in the Apache configuration
install: all
	$(APXS) -i -a -n 'webkit' mod_webkit.so

#   cleanup
clean:
	-rm -f mod_webkit.o marshal.o mod_webkit.so


#   reload the module by installing and restarting Apache
reload: install restart

#   the general Apache start/restart/stop procedures
start:
	$(APACHECTL) start
restart:
	$(APACHECTL) restart
stop:
	$(APACHECTL) stop

